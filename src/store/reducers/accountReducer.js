
const INITIAL_STATE = {
  logged: false,
  user: null,
}

function accountReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default accountReducer
